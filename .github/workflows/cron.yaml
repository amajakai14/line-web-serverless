name: hourly-cron
on:
  schedule:
    - cron: "0 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url 'https://line-web-serverless-857x.vercel.app/api/cron' \
          --header "Authorization: Bearer e0ebb420-9c53-4ccd-b2f1-607e51291f9b"
