name: hourly-cron
on:
  schedule:
    - cron: "* * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          echo $MYSECRET
        env: ${{ secrets.CRON_SECRET }}
